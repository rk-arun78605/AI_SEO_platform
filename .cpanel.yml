---
deployment:
  tasks:
    - export DEPLOYPATH=/home/indrxkhf/aisle
    - export NPM=/opt/alt/alt-nodejs22/root/usr/lib/node_modules/npm/bin/npm
    - rm -rf $DEPLOYPATH/.next $DEPLOYPATH/tsconfig.tsbuildinfo
    - $NPM ci --production=false --prefix $DEPLOYPATH
    - $NPM run build --prefix $DEPLOYPATH
